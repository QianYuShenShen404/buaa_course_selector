# BUAA选课脚本方案2 (混合架构) 依赖包
# 
# 安装说明:
# 1. 安装Python依赖: pip install -r requirements.txt
# 2. 安装Playwright浏览器: playwright install chromium
#
# Python版本要求: 3.8+

# 核心依赖
playwright>=1.20.0    # 浏览器自动化，用于自动登录认证
requests>=2.28.0      # HTTP请求库，用于高效选课

# 可选依赖 (如果需要高级功能)
# rich>=12.0.0        # 美化终端输出 (如果原项目使用了)